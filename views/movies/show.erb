<!DOCTYPE html>
<html lang="en">
  <head>
    <title> <% params[:id] %></title>
  </head>

  <h1> <%= params[:id] %> <h1>
    <body>
      <ul>
        <li> Genre: <%= @movie_info[0]["genre"] %></li>
        <li> Studio: <%= @movie_info[0]["studios"] %></li>
        <li> Rating: <%= @movie_info[0]["rating"] %></li>

      </ul>

        <ul> <% @movie_info.each do |movie| %>

          <li> Actor: <a href ="/actors/<%= movie["actor_ind_id"] %>"> <%= movie["name"] %> </a> --Role: <%= movie["character"] %>

          </li>
         <% end %>
        </ul>
    </body>
</html>
